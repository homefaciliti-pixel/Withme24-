{
  "name": "withme24-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node app.js"
  }
}
